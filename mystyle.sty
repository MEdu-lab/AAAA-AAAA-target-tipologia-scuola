% mystyle.sty
\ProvidesPackage{mystyle}

% Margini (usa il pacchetto geometry)
\RequirePackage[margin=2cm]{geometry}

% Dimensione del font
\renewcommand\normalsize{\fontsize{11}{13}\selectfont}

% Centrare i titoli di sezione
\RequirePackage{sectsty}
\sectionfont{\centering}

% Classe documento e lingua NON si mettono qui:
% - la classe (`article`) va scelta in Pandoc
% - la lingua si imposta con babel/polyglossia nel preambolo
